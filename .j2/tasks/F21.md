# F21 — `/next` Command Tasks

### T01 — Add `next` handler to runner.py
**Status**: done
**Description**: Add a special-case `next` command to runner.py main(). It reads `.j2/state.md`, extracts the `next: /command [args]` line, parses command id and optional feature arg, then delegates to the normal template pipeline for that command. Does not require a workflow.yaml entry or its own template.

### T02 — Create `/next` slash command files
**Status**: done
**Description**: Create `.claude/commands/next.md` and `scaffold/.claude/commands/next.md`, both containing `$(python3 .j2/runner.py next --root .)`.

### T03 — Write tests for `next` handler
**Status**: done
**Description**: Add tests that write a state.md with a known `next:` line, invoke the runner's next handler, and assert the output matches the delegated command's rendered template. Test both a bare command (e.g. `/task-next`) and one with a feature arg (e.g. `/tasks-gen F11`).
