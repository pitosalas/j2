# F07 — `/gen-tasks` Command

## Tasks

### T01 — Add status field to task format in gen_tasks template
**Status**: done
**Description**: The gen_tasks template formats tasks without a `**Status**` field. All task files need `**Status**: done` on each task so the runner and `/next-task` can track which tasks are done. Update the format example in the template.

### T02 — Write end-to-end rendering test for gen-tasks
**Status**: done
**Description**: Add a test that renders the gen-tasks template with a sample spec, rules, and feature section. Assert that `{{rules}}`, `{{spec}}`, and `{{feature}}` are all injected with no unresolved placeholders remaining.
