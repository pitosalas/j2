# F08 — `/refine-tasks` Command

## Tasks

### T01 — Add status preservation note to refine_tasks template
**Status**: done
**Description**: Like refine_features, the refine_tasks template should remind Claude to preserve the `**Status**` field on each task when outputting the updated task list. Add a line to that effect.

### T02 — Write end-to-end rendering test for refine-tasks
**Status**: done
**Description**: Add a test that renders the refine-tasks template with a sample task file, rules, and request. Assert that `{{tasks}}`, `{{rules}}`, and `{{request}}` are all injected with no unresolved placeholders remaining.
