# F28 — Open Source Readiness

## Tasks

### T01 — Add GitHub Actions CI workflow
**Status**: done
Create `.github/workflows/ci.yml` that runs `pytest` on every push and pull request, targeting Python 3.10, 3.11, and 3.12. Use `actions/checkout` and `actions/setup-python`. Cache pip dependencies.

### T02 — Add GitHub issue templates
**Status**: done
Create `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` using standard GitHub issue template format (YAML front matter with name, about, labels, title).

### T03 — Create CONTRIBUTING.md
**Status**: done
Write a CONTRIBUTING.md in the repo root covering: how to clone and set up a dev environment, how to run the tests (`pytest tests/`), coding standards (reference rules.md), how to submit a PR, and how to report bugs.

### T04 — Add badges to README
**Status**: done
Add a badge row at the top of README.md (below the title, above the intro paragraph) for: GitHub Actions CI status, MIT license, and Python version (3.10+). Use standard shield.io or GitHub badge URLs.

### T05 — Fix .gitignore
**Status**: done
Add `.pytest_cache/` and `*.egg-info/` to `.gitignore`. Verify `dist/` and `build/` are also covered.

### T06 — Create CHANGELOG.md
**Status**: done
Create `CHANGELOG.md` in the repo root following Keep a Changelog format (https://keepachangelog.com). Add an initial `[Unreleased]` section and a `[0.1.0]` entry summarising the 27 features shipped.

### T07 — Create SECURITY.md
**Status**: done
Create `SECURITY.md` with a supported versions table and instructions for reporting vulnerabilities privately (via GitHub private vulnerability reporting or email).

### T09 — Add linting config to pyproject.toml
**Status**: done
Add a `[tool.ruff]` section to `pyproject.toml` with sensible defaults (line-length = 100, select = ["E", "F", "I"], ignore = []), and add `ruff` to the dev dependencies. Also add a `[tool.ruff.lint]` isort section.

### T10 — Add test coverage config to pyproject.toml
**Status**: done
Add `pytest-cov` to dev dependencies in `pyproject.toml`. Add a `[tool.coverage.run]` section (source = [".j2"], omit = ["tests/*", ".venv/*"]) and a `[tool.pytest.ini_options]` addopts line with `--cov=.j2 --cov-report=term-missing`.
