# F04 — `/refresh` Command Tasks

### T01 — Add `refresh` subcommand to runner.py
**Status**: done
**Description**: Register `refresh` in workflow.yaml pointing to refresh.md template. Runner reads specs dir, injects spec and rules via placeholders, and prints the rendered prompt.

### T02 — Create `refresh.md` template
**Status**: done
**Description**: Plain-text template at `scaffold/.j2/templates/refresh.md` that instructs Claude to summarize the spec, surface ambiguities, propose answers, and rewrite the spec in a fenced code block.

### T03 — Register `/refresh` slash command
**Status**: done
**Description**: `scaffold/.claude/commands/refresh.md` and `.claude/commands/refresh.md` both exist and call `python3 .j2/runner.py refresh --root .`.

### T04 — Write test for `refresh` render
**Status**: done
**Description**: Add a test that builds a minimal project with a spec and rules file, renders the `refresh` template via the runner, and asserts that spec content, rules content, and no unresolved placeholders appear in the output. Follow the pattern of `test_gen_features_renders_spec_and_rules`.
