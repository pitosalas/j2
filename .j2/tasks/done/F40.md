# F40 — `/status` Command

## Tasks

### T01 — Create status.md template
**Status**: done
**Description**: Create `.j2/templates/status.md`. It should instruct Claude to report project health using injected context: count spec files in `{{spec}}`; show feature breakdown (done / in progress / not started) from `{{features}}`; list features missing task files from `{{missing_tasks}}`; count pending tasks by scanning `.j2/tasks/*.md` for `not started` statuses; show `completed:` and `next:` lines from `{{state}}`. Output must be concise plain text — no raw file content, no markdown tables required.

### T02 — Register status command in workflow.yaml
**Status**: done
**Description**: Add a `status` entry to `.j2/config/workflow.yaml` with `template: status.md`. No feature argument, no inline args needed.

### T03 — Add .claude/commands/status.md slash command
**Status**: done
**Description**: Create `.claude/commands/status.md` with frontmatter `description: Show current project status` and body `$(python3 .j2/runner.py status --root .)`.

### T04 — Write tests for status command
**Status**: done
**Description**: In `tests/test_status.py`, add: `test_status_template_exists` (reads `.j2/templates/status.md`), `test_status_workflow_registered` (checks `workflow.yaml` has `status` step pointing to `status.md`), `test_status_command_exists` (checks `.claude/commands/status.md` exists and calls `runner.py status`).
