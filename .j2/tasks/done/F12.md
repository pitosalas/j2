# Tasks for F12 — Slash Command Registration

### T01 — Add feature_id and target context loaders to runner
**Status**: done
**Description**: Add `--target` argument to runner.py argparse. Add `feature_id` loader (returns `args.feature` or falls back to `find_default_feature`) and `target` loader (returns `args.target`) to the `build_context` loaders dict.

### T02 — Update command files to pass $ARGUMENTS inline
**Status**: done
**Description**: Update five `.claude/commands/*.md` files to pass `$ARGUMENTS` to the runner via the appropriate flag: `tasks-gen`, `tasks-refine`, `task-start`, `milestone` use `--feature $ARGUMENTS`; `deploy` uses `--target $ARGUMENTS`. `features-refine` is excluded — it still prompts interactively because open-ended text args can't be driven by `/continue`.

### T03 — Remove interactive prompts from templates
**Status**: done
**Description**: Update five templates to remove the "Output ONLY... stop and wait" prompt pattern and replace with inline placeholder text: `gen_tasks.md`, `start_task.md`, `milestone.md` use `{{feature_id}}`; `deploy.md` uses `{{target}}`; `refine_tasks.md` removes the feature ID prompt but keeps the refinement request prompt. `refine_features.md` is excluded — it retains the interactive prompt.

### T04 — Write tests for inline argument passing
**Status**: done
**Description**: Add tests verifying: (1) runner correctly injects `{{feature_id}}` when `--feature F01` is passed, (2) runner falls back to default feature when `--feature` is omitted, (3) runner correctly injects `{{target}}` when `--target` is passed, (4) runner correctly injects `{{request}}` when `--request` is passed.
