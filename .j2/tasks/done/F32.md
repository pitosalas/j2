# F32 — `/task-run-all` Command

## Tasks

### T01 — Create `run_all_tasks.md` template
**Status**: done
**Description**: Create `.j2/templates/run_all_tasks.md`. The template receives `{{feature_id}}` and `{{rules}}`. Instructions: read the task file at `.j2/tasks/{{feature_id}}.md`, implement every task whose `**Status**` is `not started` in order — for each, write the code, update its status to `done`, then move to the next. After all tasks are done, run the test suite with `pytest`. Report a summary of what was implemented and test results. Do not modify `features.md`, `state.md`, or `README.md`.

### T02 — Register command in workflow.yaml and create slash command
**Status**: done
**Description**: Add a `task-run-all` entry to `.j2/config/workflow.yaml` pointing to `run_all_tasks.md`. Create `.claude/commands/task-run-all.md` with the standard pattern: frontmatter description and `$(python3 .j2/runner.py task-run-all --feature $ARGUMENTS --root .)`.

### T03 — Write tests for task-run-all
**Status**: done
**Description**: Add tests verifying: (1) `run_all_tasks.md` template exists and contains `{{feature_id}}` and `{{rules}}` but not `{{spec}}` or `{{features}}`, (2) the workflow.yaml entry exists and points to the correct template, (3) the slash command file exists and calls runner.py with the right arguments.
